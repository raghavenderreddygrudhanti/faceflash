__pycache__/
*.pyc
*.egg-info/
dist/
build/
.eggs/
*.onnx
.faceflash/
.venv/
rust/target/
# Datasets, embeddings and raw images live locally or on HuggingFace — NEVER in git
# (images can be 16GB; embeddings 2GB). Results go to git; embeddings to HF.
data/

# OS junk
.DS_Store
Thumbs.db

# Temporary/stale result folders
results 2/
results3/

# Rust build artifacts (built by maturin on install)
faceflash/*.so
*.so
